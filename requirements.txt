# CryptoLaunchAgent requirements (Phase 2)
# Target Python version: 3.12+
# Minimal runtime dependencies for configuration and environment loading.
# Add other dependencies in later phases as required.

# Load environment variables from a .env file during local development
python-dotenv>=1.0.0

